import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Telas em Tabs',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
